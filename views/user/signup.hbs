{{>user-header}}

{{!-- 

<div class="row">
    <div class="col-md-6 m-auto">
        <div class="container mt-5 pl-5">
          <h2 class="text-center" style="color:white;">Create new account</h2>

          <form action="/signup" method="post" id="Form">
		  {{#if error}}
		  <div class="text-warning text-center">This email and Phone number is already Exist</div>
		  {{/if}}
              <div class="form-group">
                <label>Name</label>
                <input type="text" class="form-control" name="Name" id="Name" placeholder="Enter Name">
              </div>
               <span id="errorName" style="color: crimson;"></span>

              <div>
                  <div class="form-group">
                <label>Email address</label>
                <input type="email" class="form-control" name="Email" id="Email" placeholder="Enter email">
                  </div>
                  <span id="errorEmail" style="color: crimson;"></span>
				   <div class="form-group">
                      <label>Phone Number</label>
                      <input type="tel" maxlength="10" class="form-control" name="phoneNumber" id="phoneNumber" placeholder="Phone Number">
                  </div>
                      <span id="errorNumber" style="color: crimson;"></span>
                  <div class="form-group">
                      <label>Password</label>
                      <input type="password" class="form-control" name="password" id="Password" placeholder="password">
                  </div>
                  <div id="errorPassword" style="color: crimson;"></div>
                  <div class="form-group">
                      <label>Confirm Password</label>
                      <input type="password" class="form-control" name="Cpassword" id="CPassword" placeholder="password">
                  </div>
				   <input type="hidden" name="status" required value="unblock">

               <a href="/login">Already have account?</a>
               <button type="submit" class="btn-primary float-right">Signup</button>
              
          </form>
        </div>

    </div>

</div>
<script>
        const name=document.getElementById("Name")
        const email=document.getElementById("Email");
        const password=document.getElementById("Password");
        const form=document.getElementById("Form");
        const error1=document.getElementById("errorEmail");
        const error2=document.getElementById("errorPassword")
        const error3=document.getElementById("errorName")
 
        form.addEventListener('submit',(e)=>{
          let message1=[]
          let message2=[]
          let message3=[]
          
      
          if(email.value===''||email.value==null){
            message1.push('Please enter ther email')
        }else if(email.value.indexOf('@')<=0){
          message1.push('Invaild @ position')
        }else if(email.value.charAt(email.value.length-4)!='.'){
          message1.push('Email format Incorrect')

        }
        
          if(password.value===''||password.value==null){
            message2.push('Please enter the password')
          }
           else if(password.value.length<3){
            message2.push('password must be atleast 3 character')
          }
          if(name.value===''||name.value==null){
            message3.push('please enter the name')
          }
          if(message1.length>0){
           e.preventDefault();
           error1.innerText=message1.join('  ')
          }
          if(message2.length>0){
            e.preventDefault()
            error2.innerText=message2.join('  ')
          }if(message3.length>0){
            e.preventDefault()
            error3.innerText=message3.join(' ')
          }
         
         
        })
       

</script>
 --}}



<body onload="document.registration.focus();">
<!-- partial:index.partial.html -->
<div class="login-page">
  <div class="form">
     <form action="/signup" method="post" name="myForm" onsubmit = "return(validate());">
      {{#if error}}
		  <div class="text-warning text-center">This email and Phone number is already Exist</div>
		  {{/if}}
      <input type="text" name="Name" id="Name" placeholder="name"/>
      <input type="text" name="Email" id="Email" placeholder="Email" onsubmit="return(validateEmail());"/>
      <input type="password" name="password" id="Password" placeholder="password"/>
      <input type="password" name="Cpassword" id="Cpassword" placeholder="Confirm password"/>
      <input type="hidden" name="status" required value="unblock">
     
      <button type="submit" >create</button>
      <p class="message">Already registered? <a href="/login">Sign In</a></p>
    </form>
  </div>
</div>
<!-- partial -->

</body>
</html>

<style>

.login-page {
  width: 360px;
  padding: 8% 0 0;
  margin: auto;
  margin-top: -80px;
}
.form {
  position: relative;
  z-index: 1;
  background: #FFFFFF;
  max-width: 360px;
  margin: 0 auto 100px;
  padding: 45px;
  text-align: center;
  box-shadow: 0 0 20px 0 rgba(0, 0, 0, 0.2), 0 5px 5px 0 rgba(0, 0, 0, 0.24);
}
.form input {
  font-family: "Roboto", sans-serif;
  outline: 0;
  background: #f2f2f2;
  width: 100%;
  border: 0;
  margin: 0 0 15px;
  padding: 15px;
  box-sizing: border-box;
  font-size: 14px;
}
.form button {
  font-family: "Roboto", sans-serif;
  text-transform: uppercase;
  outline: 0;
  background: #4CAF50;
  width: 100%;
  border: 0;
  padding: 15px;
  color: #FFFFFF;
  font-size: 14px;
  -webkit-transition: all 0.3 ease;
  transition: all 0.3 ease;
  cursor: pointer;
}
.form button:hover,.form button:active,.form button:focus {
  background: #43A047;
}
.form .message {
  margin: 15px 0 0;
  color: #b3b3b3;
  font-size: 12px;
}
.form .message a {
  color: #4CAF50;
  text-decoration: none;
}
.form .register-form {
  display: none;
}
.container {
  position: relative;
  z-index: 1;
  max-width: 300px;
  margin: 0 auto;
}
.container:before, .container:after {
  content: "";
  display: block;
  clear: both;
}
.container .info {
  margin: 50px auto;
  text-align: center;
}
.container .info h1 {
  margin: 0 0 15px;
  padding: 0;
  font-size: 36px;
  font-weight: 300;
  color: #1a1a1a;
}
.container .info span {
  color: #4d4d4d;
  font-size: 12px;
}
.container .info span a {
  color: #000000;
  text-decoration: none;
}
.container .info span .fa {
  color: #EF3B3A;
}
body {
  background: #76b852; /* fallback for old browsers */
  background: -webkit-linear-gradient(right, #76b852, #8DC26F);
  background: -moz-linear-gradient(right, #76b852, #8DC26F);
  background: -o-linear-gradient(right, #76b852, #8DC26F);
  background: linear-gradient(to left, #76b852, #8DC26F);
  font-family: "Roboto", sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;      
}
</style>

 <script type="text/javascript">
function validate(){
   var emailID = document.myForm.Email.value;
   var firstpassword = document.myForm.password.value;
   var secondpassword = document.myForm.Cpassword.value;
  atpos = emailID.indexOf("@");
  dotpos = emailID.lastIndexOf(".");

  if(document.myForm.Name.value == ""){
    alert("please provide your name !");
    document.myForm.Name.focus();
    return false;
  }
  if(document.myForm.Email.value == ""){
    alert("please provide your Email!");
    document.myForm.Email.focus();
    return false;
  }
   if(atpos < 1|| (dotpos - atpos < 2)){
    alert("please enter correct email ID")
    document.myForm.Email.focus();
    return false;
  }
   if(document.myForm.password.value == "" || document.myForm.password.value.length !=5){
    alert("please provide atleast 5digit password");
    document.myForm.password.focus();
    return false;
  }
  if(firstpassword == secondpassword){
    return true;
  }else{
    alert("password must be same!");
    document.myForm.Cpassword.focus();
    return false;
  }
}

</script>

